# Server
PORT=3001

# Database path (for SQLite)
# In production (Railway), set to /data for persistent volume
# DB_PATH=/data

# Database (SQLite auto-created at backend/brawl.db or $DB_PATH/brawl.db)
